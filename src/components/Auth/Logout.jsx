import { useNavigate } from "react-router-dom";
import { LogoutButton } from "../Buttons/LogoutButton";

export const Logout = () => {
    return (
        <>
          <LogoutButton/>   
        </>
    )
}