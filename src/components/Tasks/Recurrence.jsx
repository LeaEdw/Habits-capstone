// CSS Styling Imports
import "./Task.css"

// All other imports


export const RecurrenceSelector = () => {

}

export const TaskScheduler = () => {

}


// This will be a component added to the New Tasks Page View
// The user will select a checkbox (which is responsible for adding the recurring boolean to the task)

// The recurrence selector will be a dropdown menu where the user can choose a number (1-6, 1-)

// The user will be able to select the frequency of the reoccurrence ( days, weeks, months ) 
// and the numbers associated with each ( every 3 days, every 2 weeks, every 1 month )

// The task scheduler will be an option for the users who need to have a task on a specific day 
// this component will be similar to the basic create a new task, but the user will be able to set the
// start date for a time in the future